"""
Package marker for the project's `src` package.

Creating this file ensures tools and the interpreter treat `src` as a package
so imports like `import src.domain.models` resolve in editors that don't
automatically include the project root on sys.path.

This file intentionally left minimal.
"""

__all__ = []
