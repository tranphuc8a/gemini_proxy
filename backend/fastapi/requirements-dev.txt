# Development dependencies: tests, migrations and tooling.
#
# Deliberately NOT in requirements.txt. Vercel installs that file into the
# function bundle, which every package adds weight to, and none of this runs in
# production.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=7.0
pytest-asyncio>=0.22
respx>=0.20
pytest-cov>=4.0

# Migrations (see README_MIGRATIONS.md). The alembic/ folder is excluded from
# the deployed bundle, so the package is not needed there either.
alembic>=1.10
